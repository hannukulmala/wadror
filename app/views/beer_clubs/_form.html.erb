<%= form_with(model: beer_club) do |form| %>
  <% if beer_club.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block", class: "form-label" %>
    <%= form.text_field :name, class: "input-group-text"%>
  </div>

  <div>
    <%= form.label :founded, style: "display: block", class: "form-label"%>
    <%= form.number_field :founded, class: "input-group-text"%>
  </div>

  <div>
    <%= form.label :city, style: "display: block", class: "form-label" %>
    <%= form.text_field :city, class: "input-group-text"%>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
